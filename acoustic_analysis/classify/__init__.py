"""Quality classification: a reference profile of known-good clips plus a
rule-based grade against it (docs/METHODS.md section 6). A trained model is a
separate, later step that consumes the exported dataset.
"""
